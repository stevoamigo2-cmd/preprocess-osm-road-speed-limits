name: OSM Tile Preprocessor

on:
  workflow_dispatch: {}
  schedule:
    - cron: '0 2 * * *'
  push:
    paths:
      - 'regions/**'

jobs:
  build-and-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Run preprocessor
        env:
          OVERPASS_ENDPOINT: "https://overpass-api.de/api/interpreter"
          TILE_ZOOM: "15"
          THROTTLE_MS: "1200"
        run: node preprocess_osm.js

      - name: Commit tiles to gh-pages
        env:
          GH_PAT: ${{ secrets.GH_PAT2 }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "actions@github.com"

          git checkout --orphan gh-pages || git checkout gh-pages
          git rm -rf tiles || echo "No old tiles"

          git add -A tiles
          git commit -m "Update tiles from GH Action" || echo "No changes to commit"
          git push -f https://x-access-token:${GH_PAT2}@github.com/${{ github.repository }} gh-pages
